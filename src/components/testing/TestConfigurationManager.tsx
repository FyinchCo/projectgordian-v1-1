
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export const TestConfigurationManager = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Configuration Manager</CardTitle>
        <CardDescription>
          Create and manage archetype test configurations
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">Configuration management interface coming next...</p>
      </CardContent>
    </Card>
  );
};
